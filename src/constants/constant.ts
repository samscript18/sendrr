const secrets = {
	walletConnectProjectId: <string>process.env.NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID,
	draftStorageKey: <string>process.env.NEXT_PUBLIC_DRAFT_STORAGE_KEY,
	seedPhrase: <string>process.env.NEXT_PUBLIC_SEED_PHRASE,
	password: <string>process.env.NEXT_PUBLIC_PASSWORD,
};

export default secrets;
